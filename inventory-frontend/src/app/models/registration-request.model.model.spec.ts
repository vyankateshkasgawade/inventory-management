import { RegistrationRequestModel } from './registration-request.model.model';

describe('RegistrationRequestModel', () => {
  it('should create an instance', () => {
    expect(new RegistrationRequestModel()).toBeTruthy();
  });
});
